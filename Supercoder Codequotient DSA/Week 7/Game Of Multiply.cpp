#include <bits/stdc++.h>
#include <vector>
#include <algorithm>
#include <climits>
#include <unordered_map>
#include <cstring>
using namespace std;

/*
after n multiplies:
even -> +ve
odd -> -ve

therefore from [1, n] those no. which have odd no. of factors ===
all the perfect sq in [1, n] === int(sqRoot(n))
*/
#include <cmath>
int multiplyGame(int N)
{
 // Write your code here
 int ans = sqrt(N);
 return ans;
}